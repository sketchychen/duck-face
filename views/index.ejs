<h2>duckface</h2>
<div class="banner-block">
  <% if(user === undefined) { %>
    <a class="btn btn-default banner-block-button" href="/auth/signup">sign up</a> <h4>or</h4> <a class="btn btn-default banner-block-button" href="/auth/login">login</a>
  <% } else { %>
    <h3>welcome, <%= user.name %></h3>
  <% } %>
</div>
<div class="gallery">
  <div class="columns">
    <% duckfaces.forEach(function(duckface){ %>
      <div class="gallery-thumbnail">
        <img src="<%- duckface %>">
      </div>
    <% }); %>
  </div>
</div>
